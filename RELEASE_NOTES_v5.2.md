## CareEyes Pro v5.2

这是一次界面和发布体积优化更新，核心护眼、休息提醒、统计、托盘和快捷键功能保持不变。

### 更新内容

- 使用顶部紧凑导航替代宽侧边栏，主窗口占用更小。
- 收紧预设按钮、页面边距和卡片样式，常用控制更集中。
- 删除未使用的 `pywin32` 依赖，安装环境更轻。
- 使用精简 PyInstaller 配置打包，排除未使用的 Qt 模块。
- 为 EXE 增加正式的 v5.2 文件版本和产品信息。

### 下载与使用

下载 `CareEyesPro.exe` 后直接运行，无需安装 Python。Windows 首次运行可能显示 SmartScreen 提示，这是因为当前版本未进行商业代码签名。

### 校验

发布附件中提供 `CareEyesPro.exe.sha256`，可用于核对下载文件完整性。
